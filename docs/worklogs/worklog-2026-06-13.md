# Worklog - 2026-06-13

## Summary

Refined the collaboration model so Hermes, Gemini, DeepSeek, and similar helpers can contribute bounded code/plugin drafts while Codex keeps final review and merge authority.

## Changes

- Added mandatory agent rules:
  - `docs/agent-development-rules.md`
  - `docs/agent-task-template.md`
  - `docs/agent-delivery-report-template.md`
  - `docs/agent-review-checklist.md`
  - `docs/daily-agent-log-template.md`
- Added external delivery and review folders:
  - `docs/incoming/README.md`
  - `docs/reviews/README.md`
- Updated `docs/agent-operating-model.md` from read-only assistance to controlled external development.
- Updated `docs/external-toolbox-intake.md` with the Gemini toolbox path and the default decision to selectively rewrite useful tools instead of merging the toolbox directly.
- Updated `docs/next-steps.md` with no-Git delivery rules and the new agent documentation flow.
- Updated `docs/handoff-new-chat-2026-06-07.md` with the G path migration warning and the expanded Hermes/Gemini/DeepSeek rules.

## Gemini Toolbox Status

Known local path:

```text
G:\Codex\YangAgent Revit\YangAgent Revit\Gemini 资料\YangTools_SourceCode
```

Initial decision:

- Treat as external reference material.
- Do not merge directly.
- Inventory commands and classify each tool before selecting what to rewrite.
- Rebuild useful tools in YangAgent style with preview / confirmation / apply / log / Undo note.

## Repository Path Note

The user wants future development under:

```text
G:\Codex\YangAgent Revit\YangAgent Revit
```

Initial finding before migration:

- The G path currently contains `Gemini 资料`.
- At that moment, the G path had not yet been initialized as a Git repository.
- The D path remains the current complete Git repository.
- Do not delete the D path until the G path is verified as a Git repo with the latest mainline.

## Next

1. Migrate the Git repository safely to the G path.
2. Keep `Gemini 资料` local-only unless intentionally reviewed.
3. Start Gemini toolbox inventory through `docs/external-toolbox-intake.md`.
4. Continue the pyRevit sandbox MVP path before expanding feature scope.

## G Path Migration

Completed safe Git migration to:

```text
G:\Codex\YangAgent Revit\YangAgent Revit
```

Actions:

- Initialized the G path as a Git repository.
- Added `origin` as `https://github.com/5788324/Yang-Agent_Revit.git`.
- Fetched and checked out `origin/main` as local `main`.
- Added `Gemini 资料/` to `.git/info/exclude` so the external toolbox stays local-only.
- Verified `git status --short --branch` shows `main...origin/main`.

The previous D path should not be deleted unless the user explicitly confirms after checking the G path.

## Gemini Toolbox Inventory

Created:

```text
docs/reviews/gemini-toolbox-initial-inventory-2026-06-13.md
```

Initial decision:

- Do not merge the Gemini C# toolbox directly.
- Use it as a feature idea library.
- Reject current MCP auto-start and dynamic Python execution for the mainline.
- Rewrite selected useful tools in YangAgent style.

Best first candidates:

- read-only project info/reporting;
- text modification tools with dry-run reports;
- sheet/titleblock workflow after the user provides the exact work need;
- level modification only after sandbox-safe preview design.

Validation from the G path:

- `python tools\check_pyrevit_extension.py`: pass, `0 errors, 0 warnings`.
- Python compile check: pass after allowing G path write access.
- `python tools\static_checks.py --write-report`: pass, `0 errors, 11 warnings`.

## Priority Adjustment

The user confirmed that the Gemini toolbox functions are important for real work, including:

- MCP;
- micro toolbox;
- project asset manager;
- additional daily-work plugins.

Updated decision:

- Do not discard these functions.
- Do not migrate or rebuild them before the YangAgent core is usable.
- Finish the main usable project body first.
- After the core works in sandbox, bring Gemini-derived features into the controlled agent workflow and rewrite them under YangAgent safety rules.

Current sprint remains:

```text
pyRevit MVP usable in a sandbox Revit model
```

## Final Migration Check

Completed after the priority adjustment:

- G path is the active repository.
- `git status --short --branch` shows `main...origin/main`.
- Recent pushed commits include:
  - `d3357e4 docs: prioritize core MVP before Gemini migration`
  - `9b283c2 docs: record G path migration and Gemini toolbox inventory`
  - `67f6796 docs: formalize external agent workflow`
- `origin` points to `https://github.com/5788324/Yang-Agent_Revit.git`.
- `Gemini 资料/` is ignored by `.git/info/exclude`.
- The D path is clean and retained only as a safety backup.

Added new chat startup file:

```text
docs/new-chat-startup-2026-06-13.md
```

## Sandbox Preflight Follow-up

Validated from the active G path repository:

```text
G:\Codex\YangAgent Revit\YangAgent Revit
```

Issue found:

- `python tools\run_sandbox_preflight.py --write-report` was failing at `Compile offline tools`.
- Root cause was Windows refusing `.pyc` replacement under `tools\__pycache__` during `py_compile`.
- This made the preflight unreliable even though the source files themselves were valid.

Fix applied:

- Added `tools/check_offline_python_syntax.py`.
- Updated `tools/run_sandbox_preflight.py` to use source-only syntax compilation instead of `py_compile`.
- Updated `docs/sandbox-pyrevit-mvp-runbook.md` to use the active G path.
- Updated current handoff and Gemini inventory docs to reference `python tools\check_offline_python_syntax.py` for this offline check.

Validation after the fix:

- `python tools\check_offline_python_syntax.py`: pass, `0 errors, 0 warnings`.
- `python tools\run_sandbox_preflight.py --write-report`: all 7 steps `PASS`.

Current status:

- Offline preflight is now usable for the first human sandbox Revit run.
- No new feature scope was added; work stayed within precheck/documentation blocker cleanup.

## Live Sandbox Validation Status

First live Revit evidence was collected on 2026-06-13.

Verified in Revit:

- `YangAgent` tab is visible in Revit 2027.
- `System Settings` opened successfully in a sandbox model.
- The following read-only/report buttons produced files under:

```text
G:\Codex\YangAgent Revit\YangAgent Revit\Gemini 资料\Revit 测试模型\报告
```

- `Export Model Snapshot`
- `Export Regression Checklist`
- `Model Health Report`
- `Preview Duplicate Room Numbers`
- `Preview View Naming Rules`
- `Preview Unplaced Views`
- `Preview Missing Room Numbers`
- `Preview Missing Door Window Marks`

Read-only validation model used:

```text
G:\Codex\YangAgent Revit\YangAgent Revit\Gemini 资料\Revit 测试模型\项目_sandbox.rvt
```

Important limitation found:

- That model has `0` rooms, `0` doors, and `0` windows in the generated health report.
- Therefore it is valid for read-only/report verification, but not sufficient to prove `Apply Missing Door Window Marks`, `Apply Missing Room Numbers`, or Revit `Undo`.

Next richer sandbox model provided for later full apply/Undo validation:

```text
G:\Codex\YangAgent Revit\YangAgent Revit\Gemini 资料\Revit 测试模型\Snowdon Towers Sample Architectural_sandbox.rvt
```

## Framework And Governance Skeleton

Started the first YangAgent framework pass so future Gemini rewrites are forced into YangAgent-owned structure instead of becoming a parallel product line.

Implemented:

- Shared theme engine modules:
  - `pyrevit/YangAgent.extension/lib/yang_agent_settings.py`
  - `pyrevit/YangAgent.extension/lib/yang_agent_theme.py`
  - `pyrevit/YangAgent.extension/lib/yang_agent_report_style.py`
- Upgraded `System Settings` from old `light/dark` behavior to shared preset-based theme handling:
  - `yangagent_core`
  - `toolbox_warm`
  - `dark_pro`
- Added legacy compatibility mapping:
  - `light` -> `yangagent_core`
  - `dark` -> `dark_pro`
- Applied the shared report style helper to `ModelHealthReport`.
- Added framework and design-system docs:
  - `docs/framework/README.md`
  - `docs/design-system/README.md`
  - `docs/design-system/theme-engine.md`
  - `docs/design-system/external-rewrite-contract.md`
- Added governance artifacts:
  - `docs/governance/README.md`
  - `docs/governance/tool-registry.md`
  - `docs/governance/rewrite-spec-template.md`
  - `docs/governance/acceptance-gate-template.md`
  - `docs/governance/delegation-pack-template.md`
- Updated operating and helper-agent rules so Codex remains the only architecture/theme owner and external agents cannot define their own UI systems.

Validation:

- `python tools\check_offline_python_syntax.py ...`: pass on the new shared modules and updated pyRevit scripts.
- `python tools\check_pyrevit_extension.py`: pass, `0 errors, 0 warnings`.
- `python tools\static_checks.py --write-report`: pass, `0 errors, 11 warnings` (existing doc warnings only).

Current effect:

- YangAgent now has a real shared theme engine skeleton instead of isolated per-window theme logic.
- Gemini remains external, but the repository now has concrete rewrite/governance scaffolding to force future migration work into YangAgent-owned architecture.

## Document Governance Round 2

Added daily documentation operations and cleanup rules so future chats and external agents do not depend on stale context.

Implemented:

- added `docs/framework/daily-ops-routine.md`;
- updated daily handoff requirements in:
  - `README.md`
  - `docs/agent-development-rules.md`
  - `docs/agent-operating-model.md`
  - `docs/agent-task-template.md`
  - `docs/next-steps.md`
  - `docs/new-chat-startup-2026-06-13.md`
- added `docs/framework/document-governance-audit-2026-06-13.md`;
- rewrote current high-impact user-facing docs into clean current versions:
  - `docs/safety-rules.md`
  - `docs/developer-guide.md`
  - `docs/testing-and-qa.md`
  - `docs/user-guide.md`
  - `docs/view-naming-rules.md`
- downgraded older Hermes and old handoff docs into explicit historical-reference stubs;
- removed safe temporary/cache artifacts:
  - workspace `tmp/`
  - `tools/__pycache__/`
  - stray `.pyc` under the pyRevit button tree

Rule reinforced:

- every day starts with Git status plus core-doc refresh;
- every day ends with worklog, next-steps, startup prompt, and a safe Git checkpoint;
- every external helper delivery must include an operation log.

## Document Governance Round 3

Continued the governance pass on high-frequency support docs used during sandbox validation and bug triage.

Rewritten in this round:

- `docs/troubleshooting.md`
- `docs/error-codes.md`
- `docs/sandbox-snowdon-live-pack-2026-06-13.md`
- `docs/sandbox-pyrevit-mvp-runbook.md`
- `docs/sandbox-pyrevit-mvp-checklist.md`
- `docs/handoff-new-chat-2026-06-07.md`

Current effect:

- live validation and bug reporting now have one clean troubleshooting entrypoint;
- apply/preflight/C# startup failures now map to a current error-code reference;
- the Snowdon sandbox run now has a short, stable execution pack for future chats and human testing.
- daily startup, live validation, and handoff docs now point to the same current authority order instead of mixed older rules.

## Document Governance Round 4 And Repo Skeleton Audit

Continued cleanup beyond core docs so the repository can be handed off without re-reading old noise.

Rewritten or downgraded in this round:

- `docs/architecture-design.md`
- `docs/dll-addin-development-plan.md`
- `docs/agent-development-roadmap.md`
- `docs/claude-doc-integration-review.md`
- `docs/collaboration-workflow.md`
- `docs/mcp-tools.md`
- `docs/security-preparations.md`
- `docs/non-programmer-roadmap.md`
- `docs/api-and-data-schema.md`
- `docs/language-strategy.md`
- top-level `CLAUDE.md`
- top-level `HANDOFF.md`
- `mcp/revit-context/README.md`
- `samples/README.md`
- `src/README.md`
- `prompts/system/revit-agent-system-prompt.md`
- `standards/naming-rules.md`
- `standards/parameter-rules.md`

Deleted as stale draft clutter after review:

- all Hermes-only draft notes under `docs/drafts/` except:
  - `README.md`
  - `sandbox-preflight-report.md`
  - `static-check-report.md`

Project skeleton confirmed in this pass:

- pyRevit mainline lives in `pyrevit/YangAgent.extension/`
- shared pyRevit libs live under `pyrevit/YangAgent.extension/lib/`
- C# track is currently only `src/YangAgent.Revit2027/`
- offline validation is centered in `tools/`
- installer/build wrappers are centered in `scripts/`
- test fixtures are under `tests/fixtures/`
- MCP and samples directories are placeholders, not active feature surfaces

Validation:

- `python tools\static_checks.py --write-report`
- result: `0 errors, 3 warnings`
- `python tools\run_sandbox_preflight.py --write-report`
- result: `7/7 PASS`

Current effect:

- the repo now has far fewer misleading historical docs;
- most older high-noise planning files are reduced to safe stubs;
- generated evidence remains, but obsolete Hermes draft clutter has been pruned;
- future chats can recover both documentation authority and code skeleton faster.

## Document Governance Round 5 And Shared-Lib Cleanup

Continued cleanup into the remaining auxiliary layer so the repository is not only doc-clean but also less likely to propagate old wording through templates or shared code.

Rewritten in this round:

- `CHANGELOG.md`
- `pyrevit/YangAgent.extension/README.md`
- `tests/README.md`
- `prompts/task-templates/code-review.md`
- `prompts/task-templates/pyrevit-tool.md`
- `.github/PULL_REQUEST_TEMPLATE.md`
- `.github/ISSUE_TEMPLATE/bug_report.md`
- `.github/ISSUE_TEMPLATE/feature_request.md`

Implementation-level cleanup:

- rewrote `pyrevit/YangAgent.extension/lib/yang_agent_lang.py`
  - removed garbled Chinese default strings
  - kept the shared language/settings behavior stable
  - restored readable labels for language/theme prompts and local standards template text

Validation:

- `python tools\check_offline_python_syntax.py`
- result: `0 errors, 0 warnings`
- `python tools\static_checks.py --write-report`
- result: `0 errors, 3 warnings`
- `python tools\run_sandbox_preflight.py --write-report`
- result: `7/7 PASS`

Current effect:

- helper templates, GitHub templates, and extension-level README files now follow the same current project language;
- one actual shared-code corruption point was fixed instead of only rewriting docs;
- repository understanding now covers authority docs, auxiliary docs, repo skeleton, offline validators, shared pyRevit libs, and top-level templates.

## Document Governance Round 6 And Active Script Cleanup

Moved from document-only cleanup into active-script and directory-boundary cleanup.

Implemented:

- rewrote `pyrevit/YangAgent.extension/YangAgent.tab/Reports.panel/Reports.pulldown/ModelHealthReport.pushbutton/script.py`
  - removed garbled Chinese report strings
  - preserved current read-only model-health logic
- confirmed `SystemSettings.pushbutton/script.py` current user-facing picker/toast strings are already clean
- added `tools/codex_patch/README.md` so the local Codex Desktop patch script tree is explicitly treated as an environment-helper side branch, not YangAgent product code

Verification:

- targeted scan of active shared libs and high-traffic scripts for common garbled-text signatures
- `python tools\check_offline_python_syntax.py`
- result: `0 errors, 0 warnings`
- `python tools\static_checks.py --write-report`
- result: `0 errors, 3 warnings`

Current effect:

- repo cleanup is now reaching real UI/report script behavior, not only Markdown/docs;
- high-frequency shared/helper paths are clearer;
- project understanding now includes which directories are true product mainline versus local support side branches.

## Document Governance Round 7 And Active Button Surface Cleanup

Continued from shared-lib cleanup into the highest-traffic button scripts and WPF surface files.

Rewritten in this round:

- `pyrevit/YangAgent.extension/YangAgent.tab/Reports.panel/Reports.pulldown/ReportExportPath.pushbutton/script.py`
- `pyrevit/YangAgent.extension/YangAgent.tab/Reports.panel/Reports.pulldown/ExportAIReviewPrompt.pushbutton/script.py`
- `pyrevit/YangAgent.extension/YangAgent.tab/Reports.panel/Reports.pulldown/ApplyMissingDoorWindowMarks.pushbutton/script.py`
- `pyrevit/YangAgent.extension/YangAgent.tab/Reports.panel/Reports.pulldown/ApplyMissingRoomNumbers.pushbutton/script.py`
- `pyrevit/YangAgent.extension/YangAgent.tab/Settings.panel/SystemSettings.pushbutton/ui.xaml`

Reason:

- these files were active product surfaces still carrying garbled Chinese UI text or older wording;
- leaving them untouched would keep reintroducing bad UX even after the doc-governance pass.

Validation:

- `python tools\check_pyrevit_extension.py`
- result: `0 errors, 0 warnings`
- `python tools\run_sandbox_preflight.py --write-report`
- result: `7/7 PASS`
- targeted active-script garble scan across:
  - shared `lib/`
  - `SystemSettings`
  - report/export/apply pushbutton scripts
- result: no remaining common garbled-text signatures found in those high-traffic files

Current effect:

- the core pyRevit user-facing path is now much cleaner:
  - settings window
  - export-path setup
  - AI review prompt export
  - model-health report
  - both apply flows
- repository understanding now reaches both documentation authority and the primary user-facing pyRevit execution surfaces.

## ProjectInfoReport Rewrite Stabilization

Continued the first concrete Gemini-to-YangAgent rewrite by stabilizing the new read-only project info feature:

- Fixed `ProjectInfoReport.pushbutton` structure so the button is now extension-valid.
- Replaced the initial draft script with a stable read-only implementation:
  - bilingual text table;
  - shared report theme usage;
  - proper central model path conversion;
  - proper workset counting through `FilteredWorksetCollector`;
  - no model transaction or write path.
- Added a button icon so the new feature passes pyRevit extension validation.
- Rewrote `docs/new-chat-startup-2026-06-13.md` to remove the corrupted text encoding and restore a usable startup prompt.

Validation:

- `python tools\check_pyrevit_extension.py`: pass, `0 errors, 0 warnings`.
- `python tools\check_offline_python_syntax.py ...ProjectInfoReport...`: pass, `0 errors, 0 warnings`.
- `python tools\static_checks.py --write-report`: pass, `0 errors, 11 warnings` (same existing doc warnings only).

Current practical status:

- The YangAgent framework/theme/governance skeleton is now backed by a real first rewrite candidate, not only documentation.
- The next hard blocker is no longer repo validation; it is live Revit verification on the richer sandbox model:

```text
G:\Codex\YangAgent Revit\YangAgent Revit\Gemini 资料\Revit 测试模型\Snowdon Towers Sample Architectural_sandbox.rvt
```

## 1-2 Week Delivery Outlook

With the current repo state, a focused phase-1 delivery is realistic within 1-2 weeks if scope stays controlled.

Target for that window:

1. Finish the YangAgent framework skeleton:
   - shared theme presets wired into the current main windows and reports;
   - governance docs/templates kept executable and current;
   - first Gemini rewrite path established.
2. Complete live sandbox validation on the richer test model:
   - read-only reports;
   - first safe preview/apply/Undo validation path;
   - blocker capture and one blocker fix cycle.
3. Land 1-2 small rewritten Gemini-derived tools under YangAgent rules:
   - `ProjectInfoReport` read-only report;
   - one small dry-run-first text or reporting tool, if live validation does not expose a higher-priority blocker.

Not realistic in the same 1-2 week window unless scope expands:

- full Gemini toolbox migration;
- MCP production-grade integration;
- large C# framework rebuild;
- broad family / asset / batch-operation tooling.

## Fastest Live Validation Path

After the Computer Use repair path ran into Codex Desktop package drift, the fastest project-progress route was switched back to manual live validation on the richer sandbox model instead of spending more mainline time on desktop-control infrastructure.

Added:

- `docs/sandbox-snowdon-live-pack-2026-06-13.md`

Adjusted:

- `docs/sandbox-pyrevit-mvp-checklist.md`
  - now recommends the richer Snowdon Towers sandbox model and report folder explicitly;
  - now includes `Project Info Report` in the formal validation order.

Current intent:

- get the first high-value blocker from a real content-rich model quickly;
- verify the first rewritten Gemini-derived report in the same run;
- only return to Computer Use environment repair after the project mainline no longer has a faster manual path.

## Computer Use Live Revit Validation

Computer Use was recovered enough to directly control the open Revit 2027 sandbox session and validate the current YangAgent ribbon on the live Snowdon Towers model.

Live confirmation:

- Computer Use successfully attached to:

```text
Autodesk Revit 2027 - EDUCATION (NON COMMERCIAL) - [Snowdon Towers Sample Architectural_sandbox.rvt - Sheet: G000 - Cover]
```

- `YangAgent -> System Settings` opened successfully under live control.
- Theme preset dropdown was verified live and currently exposes:
  - `YangAgent Core`
  - `Toolbox Warm`
  - `Dark Pro`

First real blocker captured:

- `YangAgent -> 导出报告 -> Project Info Report` currently fails live with Revit dialog:
  - `Wrong Full Class Name`
  - class `yangagent_yangagent_reports_reports_projectinforeport` cannot be found in the add-in assembly.

Root-cause evidence:

- The runtime-generated file

```text
C:\Users\YANG\AppData\Roaming\pyRevit\2027\YangAgent.cs
```

  does not include a `projectinforeport` command class.
- The runtime-generated assembly

```text
C:\Users\YANG\AppData\Roaming\pyRevit\2027\pyRevit_2027_6e209f291442d185_YangAgent.dll
```

  also exposes existing report commands but not `ProjectInfoReport`.
- The loaded extension path is already the correct junction target:

```text
C:\Users\YANG\AppData\Roaming\pyRevit\Extensions\YangAgent.extension
-> G:\Codex\YangAgent Revit\YangAgent Revit\pyrevit\YangAgent.extension
```

Practical conclusion:

- this is now a runtime registration/cache rebuild problem, not a repo file placement problem;
- `System Settings` and the shared preset theme path are live;
- `ProjectInfoReport` still needs the pyRevit runtime command assembly to be rebuilt before it can be considered live-validated.

Immediate next step:

- rebuild or invalidate the `pyRevit 2027` YangAgent runtime cache with Revit closed, then re-open Revit and rerun `Project Info Report`.

## Daily Handoff And Logging Rule

Added a mandatory daily project-ops routine so future chats and helper agents stop losing time on repeated re-orientation.

Added:

- `docs/framework/daily-ops-routine.md`

Updated expectations:

- Every day must start with `git status --short --branch` and `git log -3 --oneline`.
- `git pull` is required only when the tree is safe, not blindly on a dirty worktree.
- Every day must end with updates to:
  - `docs/worklogs/worklog-YYYY-MM-DD.md`
  - `docs/next-steps.md`
  - `docs/new-chat-startup-YYYY-MM-DD.md`
- Every helper agent delivery must now include:
  - delivery report
  - operation log

Updated core docs:

- `docs/agent-development-rules.md`
- `docs/agent-task-template.md`
- `docs/agent-operating-model.md`
- `docs/next-steps.md`
- `docs/new-chat-startup-2026-06-13.md`

Practical clarification for current Hermes / DeepSeek work:

- `ApplyMissingDoorWindowMarks` and `ApplyMissingRoomNumbers` are existing mainline buttons that Hermes should only supplement and fix on top of current code, not rebuild from scratch.
- `ModelHealthReport` should stay as the existing button and be enhanced in place.
- Missing `View naming issues` and `Risk notes` are not satisfied by static text-only reference; they should be implemented by reusing or aligning with the existing preview logic where practical.

## Second-Round Document Governance

Started the second-round documentation governance pass.

Added:

- `docs/framework/document-governance-audit-2026-06-13.md`

Key outcomes:

- classified docs into current authority, active operational support, historical reference, drafts/generated evidence, and cleanup candidates;
- promoted `docs/framework/daily-ops-routine.md` into the practical authority set;
- identified older Hermes coordination docs as likely superseded by the new governance layer;
- identified a high-priority doc-normalization backlog for Chinese-facing files that currently display garbled in terminal review.

Safe cleanup completed in this pass:

- removed workspace `tmp/`
- removed `tools/__pycache__/`
- removed stray `.pyc` cache output under the pyRevit tree

Important note:

- the current terminal review shows several older Chinese-heavy docs with unreadable output;
- this is treated as a doc-governance and normalization issue to fix in a later pass, not as active authority to trust blindly.

## Third-Round Doc Cleanup

Continued the next doc-governance pass instead of stopping after classification only.

Completed:

- rewrote these active Chinese-facing docs into current clean versions:
  - `docs/safety-rules.md`
  - `docs/developer-guide.md`
  - `docs/testing-and-qa.md`
  - `docs/user-guide.md`
  - `docs/view-naming-rules.md`
- downgraded older Hermes coordination and old handoff docs into explicit historical-reference stubs:
  - `docs/hermes-agent-brief.md`
  - `docs/hermes-capability-assessment.md`
  - `docs/hermes-deepseek-prompt.md`
  - `docs/hermes-next-tasks.md`
  - `docs/handoff-2026-05-23.md`
  - `docs/handoff-new-chat-2026-05-23.md`

Current effect:

- new chats are less likely to pick up stale Hermes rules or old startup assumptions;
- the active user-facing and developer-facing docs are now aligned with the current sandbox MVP direction;
- the remaining historical/doc-encoding debt is narrower and easier to target in later passes.

## Document Governance Round 8 And Reports Surface Normalization

Continued the third-round cleanup from docs into the remaining live report/preview surface under the pyRevit ribbon.

Rewritten in this round:

- `pyrevit/YangAgent.extension/YangAgent.tab/Reports.panel/Reports.pulldown/PreviewDuplicateRoomNumbers.pushbutton/script.py`
- `pyrevit/YangAgent.extension/YangAgent.tab/Reports.panel/Reports.pulldown/PreviewMissingRoomNumbers.pushbutton/script.py`
- `pyrevit/YangAgent.extension/YangAgent.tab/Reports.panel/Reports.pulldown/PreviewMissingDoorWindowMarks.pushbutton/script.py`
- `pyrevit/YangAgent.extension/YangAgent.tab/Reports.panel/Reports.pulldown/PreviewUnplacedViews.pushbutton/script.py`
- `pyrevit/YangAgent.extension/YangAgent.tab/Reports.panel/Reports.pulldown/PreviewViewNamingRules.pushbutton/script.py`
- `pyrevit/YangAgent.extension/YangAgent.tab/Reports.panel/Reports.pulldown/ExportRegressionChecklist.pushbutton/script.py`
- `pyrevit/YangAgent.extension/YangAgent.tab/Reports.panel/Reports.pulldown/ExportModelSnapshot.pushbutton/script.py`
- `pyrevit/YangAgent.extension/YangAgent.tab/Reports.panel/Reports.pulldown/ProjectInfoReport.pushbutton/README.md`
- report pulldown `bundle.yaml` and multiple child-button `bundle.yaml` files

Adjusted in place:

- `pyrevit/YangAgent.extension/YangAgent.tab/Reports.panel/Reports.pulldown/ProjectInfoReport.pushbutton/script.py`
  - removed the remaining historical Gemini-facing next-step wording
  - kept the read-only project report behavior unchanged

Reason:

- several active preview/export buttons still had garbled Chinese text in the live code path;
- several button titles/tooltips were still corrupted at the `bundle.yaml` layer;
- leaving these files untouched would keep reintroducing broken UI even after earlier doc and shared-lib cleanup.

Validation:

- targeted garble rescan across `Reports.pulldown` scripts, bundle metadata, and shared libs
- result: no remaining common garbled-text signatures in the scanned active report surface
- `python tools\check_pyrevit_extension.py`
- result: `0 errors, 0 warnings`
- `python tools\run_sandbox_preflight.py --write-report`
- result: `7/7 PASS`
- `python tools\static_checks.py --write-report`
- result: `0 errors, 3 warnings` (existing doc placeholder warnings only)

Current effect:

- the full `Reports -> 导出报告` ribbon surface is now much closer to a clean handoff state;
- user-visible report/preview/apply button text is substantially normalized;
- repo understanding now covers docs, governance, shared libs, active WPF, apply flows, and the remaining high-traffic report buttons.
