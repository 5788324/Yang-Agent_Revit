# Worklog - 2026-06-13

## Summary

Refined the collaboration model so Hermes, Gemini, DeepSeek, and similar helpers can contribute bounded code/plugin drafts while Codex keeps final review and merge authority.

## Changes

- Added mandatory agent rules:
  - `docs/agent-development-rules.md`
  - `docs/agent-task-template.md`
  - `docs/agent-delivery-report-template.md`
  - `docs/agent-review-checklist.md`
  - `docs/daily-agent-log-template.md`
- Added external delivery and review folders:
  - `docs/incoming/README.md`
  - `docs/reviews/README.md`
- Updated `docs/agent-operating-model.md` from read-only assistance to controlled external development.
- Updated `docs/external-toolbox-intake.md` with the Gemini toolbox path and the default decision to selectively rewrite useful tools instead of merging the toolbox directly.
- Updated `docs/next-steps.md` with no-Git delivery rules and the new agent documentation flow.
- Updated `docs/handoff-new-chat-2026-06-07.md` with the G path migration warning and the expanded Hermes/Gemini/DeepSeek rules.

## Gemini Toolbox Status

Known local path:

```text
G:\Codex\YangAgent Revit\YangAgent Revit\Gemini 资料\YangTools_SourceCode
```

Initial decision:

- Treat as external reference material.
- Do not merge directly.
- Inventory commands and classify each tool before selecting what to rewrite.
- Rebuild useful tools in YangAgent style with preview / confirmation / apply / log / Undo note.

## Repository Path Note

The user wants future development under:

```text
G:\Codex\YangAgent Revit\YangAgent Revit
```

Current finding:

- The G path currently contains `Gemini 资料`.
- The G path is not yet a Git repository.
- The D path remains the current complete Git repository.
- Do not delete the D path until the G path is verified as a Git repo with the latest mainline.

## Next

1. Migrate the Git repository safely to the G path.
2. Keep `Gemini 资料` local-only unless intentionally reviewed.
3. Start Gemini toolbox inventory through `docs/external-toolbox-intake.md`.
4. Continue the pyRevit sandbox MVP path before expanding feature scope.
