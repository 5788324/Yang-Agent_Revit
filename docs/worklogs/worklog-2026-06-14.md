# Worklog - 2026-06-14

## Summary

Fixed the live pyRevit runtime cache blocker for `ProjectInfoReport` and verified the report against the richer Snowdon sandbox model.

## Start State

Repository:

```text
G:\Codex\YangAgent Revit\YangAgent Revit
```

Git start checks:

- `git status --short --branch`: `main...origin/main` with existing uncommitted project changes.
- `git log -3 --oneline`:
  - `57baf76 docs: add new chat startup handoff`
  - `d3357e4 docs: prioritize core MVP before Gemini migration`
  - `9b283c2 docs: record G path migration and Gemini toolbox inventory`

## Runtime Cache Fix

Problem:

- Revit previously failed on `ProjectInfoReport` with `Wrong Full Class Name`.
- The repository contained the button files, but the pyRevit runtime DLL did not expose the matching command class.

Actions:

- Confirmed `C:\Users\YANG\AppData\Roaming\pyRevit\2027\YangAgent.cs` now includes:

```text
yangagent_yangagent_reports_reports_projectinforeport
```

- Confirmed the old runtime DLL still did not expose that class.
- Closed the non-interactive Revit process created during validation.
- Ran:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\clear-pyrevit-yangagent-cache.ps1
```

- Removed:
  - `C:\Users\YANG\AppData\Roaming\pyRevit\2027\pyRevit_2027_6e209f291442d185_YangAgent.dll`
  - `C:\Users\YANG\AppData\Roaming\pyRevit\2027\YangAgent.cs`
- Reopened Revit 2027 against the Snowdon sandbox to trigger pyRevit runtime rebuild.

Result:

- New cache files were generated on `2026-06-14 10:50`.
- The rebuilt DLL now contains:

```text
yangagent_yangagent_reports_reports_projectinforeport
```

## Live Evidence

Model:

```text
G:\Codex\YangAgent Revit\YangAgent Revit\Gemini 资料\Revit 测试模型\Snowdon Towers Sample Architectural_sandbox.rvt
```

Report path:

```text
G:\Codex\YangAgent Revit\YangAgent Revit\Gemini 资料\Revit 测试模型\报告
```

Generated reports:

- `project_info_report_20260614_105341.md`
- `project_info_report_20260614_105621.md`

Important result from the latest report:

- Document: `Snowdon Towers Sample Architectural_sandbox`
- Views: `507`
- Sheets: `67`
- Levels: `18`
- Rooms: `67`
- Doors: `149`
- Windows: `172`
- Revit links: `5`

Revit journal evidence:

- `journal.0056.txt` records execution of:

```text
CustomCtrl_%CustomCtrl_%CustomCtrl_%YangAgent%Reports%Reports%ProjectInfoReport:yangagent_yangagent_reports_reports_projectinforeport
```

- It also records the `ProjectInfoReport` WPF window close event.
- No `Wrong Full Class Name` was observed after cache rebuild.

## Validation

Offline validation:

- `python tools\check_pyrevit_extension.py`: `0 errors, 0 warnings`
- `python tools\run_sandbox_preflight.py --write-report`: `7/7 PASS`

Runtime validation:

- `ProjectInfoReport` runtime registration/cache issue is fixed.
- `ProjectInfoReport` generated a real report from the Snowdon sandbox model.

Manual live validation by user:

- All report/export/preview buttons exported successfully on the Snowdon sandbox model.
- `Apply Missing Room Numbers` completed successfully.
- Revit Undo successfully reverted `Apply Missing Room Numbers`.
- `Apply Missing Door Window Marks` completed successfully.
- Revit Undo successfully reverted `Apply Missing Door Window Marks`.

Conclusion:

- The current pyRevit MVP report/preview/apply/Undo chain is live-validated on a content-rich sandbox model.

## Remaining Work

Next work should focus on review and stabilization:

1. Review generated Snowdon reports and CSVs for data quality.
2. Review Hermes/DeepSeek bounded code deliveries before any merge.
3. Fix only concrete bugs found from live use.
4. Keep documentation updates limited to worklog, next-steps, and startup handoff.

Current limitation:

- The current Codex turn did not expose a usable Computer Use control interface.
- Revit was launchable from shell and generated runtime/report evidence.
- Full button-by-button GUI validation was completed manually by the user.

## Snowdon Report And CSV Data Quality Review

Reviewed the latest generated Snowdon output under the report directory.

Latest complete output set reviewed:

- `project_info_report_20260614_111009.md`
- `model_snapshot_20260614_111022.json`
- `model_snapshot_report_20260614_111022.md`
- `rooms_20260614_111022.csv`
- `doors_windows_20260614_111022.csv`
- `sheets_views_20260614_111022.csv`
- `model_health_report_20260614_111036.md`
- `missing_door_window_marks_20260614_111100.csv`
- `duplicate_room_numbers_20260614_111109.csv`
- `view_naming_rules_20260614_111119.csv`
- `unplaced_views_20260614_111130.csv`
- `missing_room_numbers_20260614_111912.csv`
- `apply_room_numbers_20260614_111927.csv`

Good findings:

- Snapshot JSON counts are internally consistent:
  - rooms: `67`
  - doors/windows: `321`
  - sheets/views: `507`
  - levels: `18`
  - model categories: `75`
- `rooms_*.csv` row count matches snapshot room count.
- `doors_windows_*.csv` row count matches snapshot door/window count.
- `sheets_views_*.csv` row count matches snapshot sheet/view count.
- `missing_room_numbers_20260614_111912.csv` found `3` missing room numbers.
- `apply_room_numbers_20260614_111927.csv` shows the same `3` rows applied with `result=applied`.
- `missing_door_window_marks_20260614_111100.csv` contains `0` rows, matching the report summary.
- `duplicate_room_numbers_20260614_111109.csv` contains `0` rows, matching the report summary.
- `view_naming_rules_20260614_111119.csv` contains `267` naming issues.
- `unplaced_views_20260614_111130.csv` contains `127` possibly unplaced views.

Issues found:

- `model_health_report_20260614_111036.md` reported missing room numbers as `0`, while `Preview Missing Room Numbers` found `3`.
- Root cause: `ModelHealthReport` used a generic parameter reader that could convert blank text parameters into `"0"`.
- `rooms_20260614_111022.csv` showed many blank text fields as `"0"` in `department` and `comments`.
- `doors_windows_20260614_111022.csv` had `family_name` and `type_name` blank for all `321` rows.

Fix applied:

- Updated `ModelHealthReport.pushbutton/script.py` parameter reading to respect Revit `StorageType`.
- Updated `ExportModelSnapshot.pushbutton/script.py` parameter reading to respect Revit `StorageType`.
- Updated `ExportModelSnapshot.pushbutton/script.py` door/window family and type extraction with fallback helpers.
- Updated `PreviewMissingDoorWindowMarks.pushbutton/script.py` with the same family/type fallback helpers for future missing-mark rows.

Validation after fix:

- `python tools\check_offline_python_syntax.py`: `0 errors, 0 warnings`
- `python tools\check_pyrevit_extension.py`: `0 errors, 0 warnings`
- `python tools\static_checks.py --write-report`: `0 errors, 3 warnings`

Required follow-up:

- Done by user: re-ran `Model Health Report` and `Export Model Snapshot` in Revit on the Snowdon sandbox.
- Reviewed regenerated `model_health_report_20260614_113012.md`.
- Reviewed regenerated `model_snapshot_20260614_113025.json`.
- Reviewed regenerated `rooms_20260614_113025.csv`.
- Reviewed regenerated `doors_windows_20260614_113025.csv`.
- Reviewed regenerated `sheets_views_20260614_113025.csv`.

Regenerated output review:

- `model_health_report_20260614_113012.md` reports `3` missing room numbers.
- The user confirmed those `3` missing room numbers were manually created for validation, because the base Snowdon model is otherwise clean for this check.
- Health report total issue count is internally consistent: `3` missing room numbers + `127` possibly unplaced views = `130`.
- Regenerated snapshot counts remain internally consistent:
  - rooms: `67`
  - doors/windows: `321`
  - sheets/views: `507`
  - levels: `18`
  - model categories: `75`
- Regenerated `rooms_20260614_113025.csv` has `0` blank text fields converted to `"0"`.
- Regenerated `rooms_20260614_113025.csv` correctly shows `3` blank room numbers for the manual validation case.
- Regenerated `doors_windows_20260614_113025.csv` has `0` blank `family_name` values and `0` blank `type_name` values.
- Regenerated `doors_windows_20260614_113025.csv` sample data includes real values such as `Door-Passage-Single-Flush` and `36" x 84"`.
- `sheets_views_20260614_113025.csv` row count matches the snapshot count. Blank `number` values on view rows and blank `view_type` / `is_template` values on sheet rows are expected from the mixed sheet/view export shape.

Follow-up fix:

- Removed duplicate bullet markers inside the snapshot summary status block; future snapshot Markdown should render summary list text without an extra leading hyphen.

Validation after follow-up fix:

- `python tools\check_offline_python_syntax.py`: `0 errors, 0 warnings`
- `python tools\check_pyrevit_extension.py`: `0 errors, 0 warnings`

## Apply Tool Safety Review

Reviewed the two model-modifying apply buttons after Snowdon live validation:

- `ApplyMissingRoomNumbers.pushbutton/script.py`
- `ApplyMissingDoorWindowMarks.pushbutton/script.py`

Safety gate status:

- Both tools restrict source CSV filenames to the matching preview export prefix.
- Both tools validate required CSV fields before any model write.
- Both tools filter rows by `dry_run=true` and valid `element_id`.
- Both tools hard-stop on duplicate `element_id` values in the apply CSV.
- Both tools print a pre-apply ElementId review before confirmation.
- Both tools require explicit user confirmation before applying.
- Both tools write model changes inside one Revit Transaction.
- Both tools write Markdown and CSV execution logs.
- Both tools include Undo / rollback instructions in the output and log.

Follow-up fix:

- Updated `pyrevit/YangAgent.extension/lib/yang_agent_apply.py` so shared apply logging reads Revit parameter text with StorageType-aware behavior.
- This prevents blank string parameters from being logged as `"0"` in future apply logs.

Validation:

- `python tools\check_pyrevit_extension.py`: `0 errors, 0 warnings`
- `python tools\check_offline_python_syntax.py`: `0 errors, 0 warnings`
- `python tools\static_checks.py --write-report`: `0 errors, 3 existing warnings`

## Shared Library Smoke Review

Reviewed the active shared Python helper layer:

- `yang_agent_settings.py`
- `yang_agent_theme.py`
- `yang_agent_report_style.py`
- `yang_agent_apply.py`
- `yang_agent_lang.py`

Smoke-test result:

- Imported all five shared modules successfully from normal Python.
- Confirmed legacy theme value `light` maps to `yangagent_core`.
- Confirmed legacy theme value `dark` maps to `dark_pro`.
- Confirmed themed report status block rendering returns HTML without import errors.

Full preflight:

- `python tools\run_sandbox_preflight.py --write-report`: all checks `PASS`.

## Hermes ModelHealthReport Delivery Review

Reviewed Hermes delivery:

- `G:\Hermes Agent\YangAgent Revit\YangAgent Revit\docs\drafts\2026-06-13_hermes_model-health-report_delivery\delivery-report.md`
- `G:\Hermes Agent\YangAgent Revit\YangAgent Revit\docs\drafts\2026-06-13_hermes_model-health-report_delivery\script.py`

Decision:

- Did not copy Hermes `script.py` directly.
- Accepted the business intent after Codex correction.
- Integrated a corrected view naming check and risk notes into the current mainline `ModelHealthReport.pushbutton/script.py`.

Important review findings:

- Hermes correctly kept the feature read-only and used `get_view_naming_rules()`.
- Direct replacement would have regressed the already validated `StorageType` parameter-reader fix.
- Direct replacement had a naming label key bug.
- Codex kept the tool read-only and did not add any Transaction.

Review record:

- `docs/reviews/hermes-model-health-report-delivery-review-2026-06-14.md`

Validation after integration:

- `python tools\check_pyrevit_extension.py`: `0 errors, 0 warnings`
- `python tools\check_offline_python_syntax.py`: `0 errors, 0 warnings`
- `python tools\static_checks.py --write-report`: `0 errors, 3 existing warnings`
- `python tools\run_sandbox_preflight.py --write-report`: all checks `PASS`

Required live follow-up:

- Re-run `Model Health Report` in the Snowdon sandbox.
- Confirm the report includes `View Naming Check`.
- Confirm the report includes `Risk Notes`.
- Confirm the issue count now includes view naming issues.

Live follow-up result:

- User re-ran `Model Health Report` in the Snowdon sandbox.
- Reviewed `model_health_report_20260614_114420.md`.
- The report includes `View Naming Check`.
- The report includes `Risk Notes`.
- Counts are internally consistent:
  - total issue count: `397`
  - manually created missing room numbers: `3`
  - possibly unplaced views: `127`
  - view naming issues: `267`
  - equation: `3 + 127 + 267 = 397`
- View naming detail:
  - checked views: `267`
  - blank names: `0`
  - temporary keywords: `2`
  - missing recommended prefix: `265`
- Risk notes correctly flag only:
  - unplaced views: `127`
  - view naming issues: `267`
- Risk notes correctly do not flag the manually created `3` missing room numbers because the threshold is `5`.

## Export Model Snapshot Follow-up Validation

User re-ran `Export Model Snapshot` in the Snowdon sandbox.

Reviewed latest output:

- `model_snapshot_report_20260614_114842.md`
- `model_snapshot_20260614_114842.json`
- `rooms_20260614_114842.csv`
- `doors_windows_20260614_114842.csv`
- `sheets_views_20260614_114842.csv`

Result:

- Snapshot Markdown no longer contains duplicate bullet markers in the summary block.
- JSON counts remain consistent:
  - rooms: `67`
  - doors/windows: `321`
  - sheets/views: `507`
  - levels: `18`
  - model categories: `75`
- CSV row counts match JSON counts.
- `rooms_20260614_114842.csv` has `0` blank text fields converted to `"0"`.
- `doors_windows_20260614_114842.csv` has `0` blank `family_name` values and `0` blank `type_name` values.

Project direction note:

- The user is under real work pressure and needs the Gemini/MCP capabilities, not more polishing of already validated buttons.
- Stop expanding or repeatedly polishing the current pyRevit report buttons unless a concrete blocker appears.
- Next execution should pivot to rebuilding the most urgently useful Gemini/MCP capability inside YangAgent standards.

## Gemini Migration Acceleration

User clarified the desired direction:

- Continue the project, but accelerate.
- Gemini plugin capabilities should be migrated as a whole over time, not ignored.
- Do not merge Gemini directly because the implementation quality is below YangAgent standards.
- First priority is theme switching and a unified migration framework so future Gemini tools do not become harder to absorb.

Actions taken:

- Verified `System Settings` XAML is valid UTF-8; apparent mojibake is PowerShell display, not file corruption.
- Added C# WPF theme bridge:
  - `src/YangAgent.Revit2027/DesignSystem/YangAgentTheme.cs`
- The C# theme bridge reads the shared `%APPDATA%\YangAgent_Revit\settings.json` and maps `theme_id` / legacy `theme` to the same three themes as pyRevit:
  - `yangagent_core`
  - `toolbox_warm`
  - `dark_pro`
- Updated `docs/governance/tool-registry.md` with migration waves:
  - wave 0: theme and design system
  - wave 1: read-only reports and audits
  - wave 2: text and annotation tools
  - wave 3: sheets, views, levels, visibility
  - wave 4: MCP and automation shell
  - wave 5: high-risk family, geometry, generation tools

Validation:

- `dotnet build src\YangAgent.Revit2027\YangAgent.Revit2027.csproj -v:minimal`: build succeeded.
- Build warning remains: Revit/.NET `Microsoft.VisualBasic` reference version conflict, `0` errors.

## Hermes V1 Migration Board

Implemented the V1 task split and Hermes handoff plan.

Created main-repo authority copy:

- `docs/governance/codex-hermes-v1-migration-board-2026-06-14.md`

Copied active Hermes execution board to:

- `G:\Hermes Agent\YangAgent Revit\YangAgent Revit\docs\drafts\codex-hermes-v1-migration-board-2026-06-14.md`

The board defines:

- V1 remaining task modules: `36` tasks across `8` modules.
- Codex as architecture, review, merge, and validation owner.
- Hermes / DeepSeek as task-package implementers only.
- Batch task package mode, not long-running independent branch mode.
- Current active queue:
  - `HERMES-W0-001 Theme Compliance Audit`
  - `HERMES-W2-001 Gemini Text Tools Inventory`
  - `HERMES-W2-002 TextModifier Preview Draft` blocked until W2-001 review

Updated:

- `docs/governance/tool-registry.md`
- `docs/next-steps.md`

New execution rule:

- Codex should not write large ordinary feature implementations by default.
- Hermes writes bounded drafts and reports.
- Codex reviews, merges, fixes blockers, validates, and records decisions.

## Hermes W0/W2-001 Delivery Review

Reviewed Hermes deliveries:

- `G:\Hermes Agent\YangAgent Revit\YangAgent Revit\docs\drafts\2026-06-14_hermes_theme-compliance-audit.md`
- `G:\Hermes Agent\YangAgent Revit\YangAgent Revit\docs\drafts\2026-06-14_hermes_text-tools-inventory.md`

Review decision:

- `HERMES-W0-001 Theme Compliance Audit`: `accepted`
- `HERMES-W2-001 Gemini Text Tools Inventory`: `accepted`

Review record:

- `docs/reviews/hermes-v1-board-deliveries-review-2026-06-14.md`

Result:

- W0 found `0` hard-coded color violations outside accepted theme definition files.
- W2 recommended `TextModifierCommand` as P0; Codex accepted this as the first Wave 2 rewrite.
- `HERMES-W2-002 TextModifier Preview Draft` is now authorized in the main-repo board.

Sync note:

- Main-repo board was updated.
- Attempt to copy the updated board to Hermes workspace was blocked by Codex usage-limit approval review.
- Until sync is retried, Hermes should read the main-repo authority copy directly:
  - `G:\Codex\YangAgent Revit\YangAgent Revit\docs\governance\codex-hermes-v1-migration-board-2026-06-14.md`

## Hermes W2-002 TextModifier Preview Review

Reviewed Hermes delivery:

- `G:\Hermes Agent\YangAgent Revit\YangAgent Revit\docs\drafts\2026-06-14_hermes_textmodifier-preview_delivery`

Decision:

- `accepted with Codex integration fixes`

Mainline integration:

- Added `pyrevit/YangAgent.extension/YangAgent.tab/Reports.panel/Reports.pulldown/PreviewTextFindReplace.pushbutton/`.
- Added missing `icon.png`.
- Replaced unreadable Chinese mojibake text with readable Chinese UI/report strings.
- Replaced raw boolean case selection with `CommandSwitchWindow`.
- Ensured Markdown output shows both current text and proposed text.
- Preserved replacement text exactly instead of stripping leading/trailing spaces.
- Kept behavior preview-only: no Transaction and no model write.

Review record:

- `docs/reviews/hermes-textmodifier-preview-delivery-review-2026-06-14.md`

Validation:

- `python tools\check_pyrevit_extension.py`: `0 errors`, `0 warnings`.
- `python tools\static_checks.py --write-report`: `0 errors`, `3` pre-existing docs warnings.
- Safety grep found no model-write code in the script.

Next gate:

- User should run `Preview Text Find Replace` once in Snowdon sandbox and send the generated CSV/Markdown.
- Do not authorize `HERMES-W2-003 TextModifier Apply Draft` until the preview output is reviewed.

## Hermes W2-003 TextModifier Apply Review

Reviewed Hermes revision delivery:

- `G:\Hermes Agent\YangAgent Revit\YangAgent Revit\docs\drafts\2026-06-14_hermes_W2-003_revision1_delivery`

Decision:

- `accepted with Codex integration fixes`

Mainline integration:

- Added `pyrevit/YangAgent.extension/YangAgent.tab/Reports.panel/Reports.pulldown/ApplyTextFindReplace.pushbutton/`.
- Kept ASCII button title to reduce pyRevit runtime registration risk.
- Preserved low-risk direct-apply flow:
  - impact summary before write;
  - one confirmation;
  - one named Transaction;
  - Markdown + CSV log;
  - Undo note.
- Added a localized `empty` text label in mainline integration.
- Reused the preview icon as the placeholder icon.

Review record:

- `docs/reviews/hermes-textmodifier-apply-delivery-review-2026-06-14.md`

Validation:

- `python tools\check_pyrevit_extension.py`: `0 errors`, `0 warnings`.
- `python tools\static_checks.py --write-report`: `0 errors`, `4` pre-existing docs warnings.

Next gate:

- User should run `Apply Text Find Replace` once in the sandbox model and verify both exported logs and full Revit Undo.
- Hermes may continue to `HERMES-W2-004 MergeText Inventory`.

## Gemini Restoration Governance

Added two governance docs for the next migration phase:

- `docs/governance/gemini-feature-restoration-map-2026-06-14.md`
- `docs/governance/gemini-plugin-authoring-spec-2026-06-14.md`

Updated Hermes control docs so future deliveries must follow:

- functionality restoration targets by panel and tool family;
- migration-friendly authoring rules for Gemini-origin work;
- the new queue starting from `HERMES-W2-004`.

## Hermes W2-004 MergeText Inventory Review

Reviewed Hermes delivery:

- `G:\Hermes Agent\YangAgent Revit\YangAgent Revit\docs\drafts\2026-06-14_hermes_W2-004_delivery`

Decision:

- `accepted with direction correction`

Accepted conclusion:

- `MergeText` is high-risk because it deletes `N-1` `TextNote` elements.
- Initial Codex review required a preview-first path.

Important review finding:

- Hermes got the main risk classification right, but missed a Gemini implementation inconsistency:
  - the Gemini checkbox means `use newline separator`;
  - the command code appears to invert that meaning.
- YangAgent must not copy that behavior blindly.
- The preview output must show the exact merged text result before any apply package is accepted.

Review record:

- `docs/reviews/hermes-merge-text-inventory-review-2026-06-14.md`

Later user direction change:

- User explicitly changed the workflow for personal use: do not add a preview step for `MergeText`.
- New execution rule for this tool: direct model modification is allowed if it is wrapped in one clear Revit Transaction and remains Undo-friendly.
- Hermes next package was changed from preview-first to direct-apply `ApplyMergeText.pushbutton`.

## Hermes W2-005 ApplyTextMerge Review

Reviewed Hermes delivery:

- `G:\Hermes Agent\YangAgent Revit\YangAgent Revit\docs\drafts\2026-06-14_hermes_W2-005_delivery`

Decision:

- `needs changes`

Main blocking findings:

- the current Transaction can still commit a partial-success state because delete/write failures are caught and not re-raised;
- the confirmation dialog does not actually show the merged text preview promised by the delivery report.

Required correction:

- keep the no-preview direct-apply direction;
- keep one named Transaction;
- make the apply path all-or-nothing;
- include the actual merged text in the confirmation summary.

Review record:

- `docs/reviews/hermes-apply-text-merge-review-2026-06-14.md`

Revision 2 follow-up:

- Hermes fixed the two original blockers:
  - merged text preview is now shown in confirmation;
  - the apply path now attempts all-or-nothing rollback.
- New blockers remain:
- the revision uses a `revit.Transaction(doc, "...")` construction pattern that is not the current YangAgent mainline pattern and was not yet validated here;
- the rollback failure path still does not export a durable Markdown/CSV failure log.

Revision 3 result:

- Hermes fixed the remaining blockers on `ApplyTextMerge.pushbutton`.
- The script now uses the same `with revit.Transaction("[Agent] ...")` pattern as the current mainline apply tools.
- The rollback path now exports durable Markdown and CSV logs.
- Review decision upgraded to `accepted`.

Next Hermes package:

- `HERMES-W2-006 DimTextOverride Inventory`

## Hermes W2-006 DimTextOverride Inventory Review

Reviewed Hermes delivery:

- `G:\Hermes Agent\YangAgent Revit\YangAgent Revit\docs\drafts\2026-06-14_hermes_W2-006_delivery`

Decision:

- `accepted with implementation direction`

Codex decisions:

- `DimTextOverride` may stay direct-apply under the current no-preview project rule.
- Replace mode must carry a stronger warning because it can mislead production documentation.
- Replace and clear stay in one button instead of being split into separate tools.

Next Hermes package:

- `HERMES-W2-007 ApplyDimTextOverride Draft`

## Hermes W2-007 ApplyDimTextOverride Review

Reviewed Hermes delivery:

- `G:\Hermes Agent\YangAgent Revit\YangAgent Revit\docs\drafts\2026-06-14_hermes_W2-007_delivery`

Decision:

- `needs changes`

Main conclusion:

- the overall direct-apply workflow is acceptable and close to merge-ready;
- one blocking issue remains: failure CSV/row logs do not populate the required `mode` field.

Required correction:

- keep the current one-button direct-apply design;
- keep replace/clear combined;
- only fix failure log rows so they carry `mode=replace` or `mode=clear`.

Revision follow-up:

- Hermes fixed the failure-log `mode` field.
- Review decision upgraded to `accepted`.

Next Hermes package:

- `HERMES-W2-008 Align/Distribute Inventory`

## Hermes W2-008 Align / Distribute Inventory Review

Reviewed Hermes delivery:

- `G:\Hermes Agent\YangAgent Revit\YangAgent Revit\docs\drafts\2026-06-14_hermes_W2-008_delivery`

Decision:

- `accepted with implementation direction`

Codex decision:

- `DistributeText` is the first migration target in this family.
- `AlignTextToText` stays second.
- `AlignTextToLine` stays last or deferred because of `PickPoint` interaction complexity.

Next Hermes package:

- `HERMES-W2-009 ApplyTextDistribute Draft`

## Hermes W2-009 ApplyTextDistribute Review

Reviewed Hermes delivery:

- `G:\Hermes Agent\YangAgent Revit\YangAgent Revit\docs\drafts\2026-06-14_hermes_W2-009_delivery`

Decision:

- `needs changes`

Main blocker:

- descending distribution modes (`x_desc`, `y_desc`) currently still move in the positive axis direction, so the resulting layout contradicts the selected mode.

Recommended same-pass correction:

- keep intended target coordinates in failure logs instead of zeroing them out.

Revision follow-up:

- Hermes fixed the descending-direction sign logic.
- Hermes also fixed failure logs to preserve intended target coordinates and deltas.
- Review decision upgraded to `accepted`.

Next Hermes package:

- `HERMES-W3-001 Sheets/Views/Levels/Visibility Inventory`

## Hermes W3-001 Wave 3 Inventory Review

Reviewed Hermes delivery:

- `G:\Hermes Agent\YangAgent Revit\YangAgent Revit\docs\drafts\2026-06-14_hermes_W3-001_delivery`

Decision:

- `accepted with implementation direction`

Codex decision:

- `SectionByLine` is the first Wave 3 migration target.
- `ViewGraphicCleaner` stays behind it until the underlying window logic is actually inspected.
- `SheetManager`, `VisibilityCopier`, and `LevelModifier` remain deferred for current V1.

Next Hermes package:

- `HERMES-W3-002 ApplySectionByLine Draft`

## Hermes W3-002 ApplySectionByLine review

- Reviewed Hermes delivery at `G:\Hermes Agent\YangAgent Revit\YangAgent Revit\docs\drafts\2026-06-14_hermes_W3-002_delivery`.
- Decision: `accepted`.
- The implementation matches the current YangAgent personal-use direct-apply rule:
  - single pre-selected line;
  - explicit confirmation;
  - one named transaction;
  - Markdown plus CSV logs;
  - Undo reminder.
- The section transform and bounding-box math align closely with the original Gemini `SectionByLineCommand.cs`.
- Residual live risk remains around live `ViewSection.CreateSection(...)` behavior and post-transaction export-path failures, so Codex should validate it in sandbox after integration.

Next Hermes package:

- `HERMES-W3-003 ViewGraphicCleaner Logic Inventory`

## Hermes W3-003 ViewGraphicCleaner inventory review

- Reviewed Hermes delivery at `G:\Hermes Agent\YangAgent Revit\YangAgent Revit\docs\drafts\2026-06-14_hermes_W3-003_delivery`.
- Decision: `accepted with direction correction`.
- Hermes correctly traced the real logic into `ViewGraphicCleanerWindow.xaml.cs` and confirmed that the core business operation is only element-level override reset through `SetElementOverrides`.
- Hermes also correctly separated the simple active-view cleaning logic from the much heavier Gemini WPF multi-view manager.
- Codex corrected one product-direction point:
  - do not split this into preview plus apply for V1;
  - current YangAgent migration rule is direct-apply by default for these personal-use tools.
- Approved V1 target:
  - one active-view direct-apply button;
  - no multi-view manager;
  - no WPF shell migration.

Next Hermes package:

- `HERMES-W3-004 ApplyViewGraphicClean Draft`

## Hermes W3-004 ApplyViewGraphicClean review

- Reviewed Hermes delivery at `G:\Hermes Agent\YangAgent Revit\YangAgent Revit\docs\drafts\2026-06-14_hermes_W3-004_delivery`.
- Decision: `accepted`.
- The implementation matches the current YangAgent direct-apply rule:
  - active-view scope only;
  - count before confirmation;
  - one named transaction;
  - Markdown plus CSV logs;
  - Undo reminder.
- Hermes kept the migration boundary correct:
  - element-level overrides only;
  - no category VG, filters, templates, hide/unhide, or Gemini WPF multi-view manager migration.
- Residual live risk remains around `OverrideGraphicSettings.InvalidPenNumber` access in IronPython and late export-path failure after a successful transaction.

Next Hermes package:

- `HERMES-W2-010 ApplyAlignTextToText Draft`

## Hermes W2-010 ApplyAlignTextToText review

- Reviewed Hermes delivery at `G:\Hermes Agent\YangAgent Revit\YangAgent Revit\docs\drafts\2026-06-14_hermes_W2-010_delivery`.
- Decision: `needs changes`.
- Main blocker:
  - mode-selection cancel currently falls through to `left`;
  - this means canceling the alignment-mode picker can still continue into a direct-apply model-changing workflow.
- Required fix:
  - cancel must exit immediately with no model change;
  - no implicit default align mode is allowed after user cancellation.
- Recommended same-pass cleanup:
  - stop reporting `OK` for target notes that have no valid bounding box in the active view.

Next Hermes package:

- `HERMES-W2-010 revision 1`

## Hermes W2-010 revision 1 review

- Reviewed Hermes revision at `G:\Hermes Agent\YangAgent Revit\YangAgent Revit\docs\drafts\2026-06-14_hermes_W2-010_delivery`.
- Decision: `accepted`.
- The prior blocker is fixed:
  - canceling mode selection now exits with no model change;
  - no implicit align-mode fallback remains.
- The target bounding-box issue is also handled correctly:
  - missing bounding boxes are detected before opening a transaction;
  - the tool stops and reports the affected element ids.
- The tool remains within the intended V1 boundary: selection-scoped, position-only, one transaction, log export, Undo reminder.

Next Hermes package:

- `HERMES-W1-001 ChineseCheck Logic Inventory`

## Hermes W1-001 ChineseCheck inventory review

- Reviewed Hermes delivery at `G:\Hermes Agent\YangAgent Revit\YangAgent Revit\docs\drafts\2026-06-14_hermes_W1-001_delivery`.
- Decision: `accepted with implementation direction`.
- Hermes correctly split the Gemini feature into:
  - a useful read-only Chinese/CJK audit path;
  - an unsafe delete path built around batch `doc.Delete(...)`.
- Codex decision:
  - only the audit half enters YangAgent V1;
  - the delete half is rejected for current migration.
- This should proceed now as a small read-only preview tool instead of waiting behind heavier Wave 3 work.

Next Hermes package:

- `HERMES-W1-002 PreviewChineseContent Draft`

## Hermes W1-002 PreviewChineseContent review

- Reviewed Hermes delivery at `G:\Hermes Agent\YangAgent Revit\YangAgent Revit\docs\drafts\2026-06-14_hermes_W1-002_delivery`.
- Decision: `accepted`.
- The tool remains fully read-only:
  - no transaction;
  - no model write;
  - no delete path.
- Hermes kept the migration boundary correct:
  - no Gemini WPF four-panel window migration;
  - no destructive cleanup path;
  - one compact preview tool only.
- The scan coverage is good enough for current V1 and the report output reuses shared theme/report helpers.

Next Hermes package:

- `HERMES-W3-005 VisibilityCopier Logic Inventory`

## Hermes W3-005 VisibilityCopier inventory review

- Reviewed Hermes delivery at `G:\Hermes Agent\YangAgent Revit\YangAgent Revit\docs\drafts\2026-06-14_hermes_W3-005_delivery`.
- Decision: `accepted with implementation direction`.
- Hermes correctly identified that the tool performs overwrite-style cross-view propagation for:
  - category hidden state and overrides;
  - filter assignment, visibility, and overrides;
  - workset visibility.
- Hermes also correctly identified that the Gemini implementation lacks diff preview and per-target logs.
- Codex decision:
  - this tool may enter V1, but only through a stronger exception workflow;
  - preview must come first;
  - any future apply must be reduced to one source view and one target view.

Next Hermes package:

- `HERMES-W3-006 ApplyVisibilityCopy Draft`

## Direct-apply policy update

- User explicitly removed the preview-first requirement for this personal-use project.
- Working rule is now:
  - direct-apply by default;
  - confirmation required;
  - named transaction required;
  - durable Markdown/CSV log required;
  - explicit Undo note required.
- This also changes the `VisibilityCopier` migration direction:
  - do not start with `PreviewVisibilityCopy`;
  - start with a reduced direct-apply tool instead;
  - keep it to `1 source -> 1 target` for current V1.

## Mainline integration batch 1

- Integrated the following accepted Hermes buttons into the live mainline extension under `pyrevit\YangAgent.extension\YangAgent.tab\Reports.panel\Reports.pulldown`:
  - `ApplyTextMerge.pushbutton`
  - `ApplyDimTextOverride.pushbutton`
  - `ApplyTextDistribute.pushbutton`
  - `PreviewChineseContent.pushbutton`
- Excluded draft `pyc` artifacts and copied only:
  - `bundle.yaml`
  - `script.py`
  - `README.md`
  - `icon.png`
- Validation after landing:
  - `python tools\check_pyrevit_extension.py`
  - result: `0 errors`, `0 warnings`
- This means the buttons are now present in the mainline extension tree, though live Revit validation is still pending.
