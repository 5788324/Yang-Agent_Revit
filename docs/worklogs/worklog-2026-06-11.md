# Worklog - 2026-06-11

## Context

Codex continued the Hermes follow-up and sandbox-packaging track.

Main decisions:

- Hermes Round 1 is not accepted from screenshot evidence alone.
- Round 2 remains blocked until Hermes fills the required structured draft files.
- The sandbox live-run pack should now include a separate compact checklist in addition to the full runbook.
- `yang_agent_apply.py` is now treated as a stable shared helper surface, not a trigger for deeper refactoring before live Revit evidence exists.

## Completed

- Added Round 1 intake draft files:
  - `docs/drafts/hermes-code-review-apply-tools.md`
  - `docs/drafts/hermes-feature-review-sandbox-runbook.md`
- Updated `docs/hermes-next-tasks.md` to mark Round 1 as:
  - reviewed by Codex
  - follow-up required
  - blocked from Round 2 until the two structured report files are completed
- Added compact sandbox operator checklist:
  - `docs/sandbox-pyrevit-mvp-checklist.md`
- Updated `docs/sandbox-pyrevit-mvp-runbook.md` to point operators to the compact checklist.
- Tightened `docs/sandbox-pyrevit-mvp-feedback-template.md` so live blocker reports now include:
  - runbook step number
  - first failing button in sequence
  - preflight report path
  - static check warning count
  - export directory path
  - whether the blocker prevents later steps
- Updated handoff and next-steps docs to treat the sandbox execution pack as:
  - full runbook
  - compact checklist
  - feedback template
  - one-command preflight report

## Validation

- Ran `python tools\check_pyrevit_extension.py`.
  - Result: 0 errors, 0 warnings.
- Ran `python tools\run_sandbox_preflight.py --write-report`.
  - Result: all preflight steps passed.
  - Report: `docs/drafts/sandbox-preflight-report.md`.
- Ran `python tools\static_checks.py --write-report`.
  - Result: 0 errors, 11 warnings.
  - Remaining warnings are still older docs/drafts items, not new blockers from this change.
- Ran `git diff --check`.
  - Result: passed, with the existing CRLF warning on generated `docs/drafts/static-check-report.md`.

## Hermes Review Gate

- Codex did not accept screenshot-only Round 1 delivery.
- Hermes later filled the two required structured Round 1 files under `docs/drafts/`.
- Round 1 disposition is now:
  - `accepted with low-risk follow-up backlog`
- Remaining open items are low-risk consistency improvements and do not block the sandbox MVP.

## Next Steps

1. Use the compact checklist plus feedback template during the first real sandbox-model run.
2. Fix only the first real live blocker after that run.
3. When the user provides the Gemini C# toolbox path, run the isolated intake process from `docs/external-toolbox-intake.md`.
4. Keep Revit 2022-2027 and MCP controlled read/write as later roadmap goals, not this week's delivery target.

## Evening Scope Reset

The user confirmed the project should be simplified around a personal assistant rather than a company or commercial platform.

New active framing:

- build a personal Revit AI assistant;
- prioritize pyRevit sandbox usefulness;
- keep Revit 2022-2027 and MCP controlled model read/write as later goals;
- treat Gemini, Hermes, and DeepSeek as auxiliary execution models;
- keep Codex responsible for project management, quality review, and key blocker fixes.

New active documents:

- `docs/product-brief.md`
- `docs/simple-roadmap.md`
- `docs/project-rules.md`
- `docs/agent-operating-model.md`
- `docs/external-toolbox-intake.md`

The one-week goal remains:

```text
pyRevit MVP usable in a sandbox model
```

## Backup And Validation Before Git Push

- Created local backup:
  - `D:\codex\backups\YangAgent_Revit_20260611_221201.zip`
  - Files included: 180
  - Excluded `.git`, Revit model files, `__pycache__`, zip files, and local export folders.
- Ran `python tools\check_pyrevit_extension.py`.
  - Result: 0 errors, 0 warnings.
- Ran `python tools\run_sandbox_preflight.py --write-report`.
  - Result: all 7 steps passed.
- Ran `python tools\static_checks.py --write-report`.
  - Result: 0 errors, 11 historical warnings.
- Ran `git diff --check`.
  - Result: passed, with the existing CRLF warning on generated `docs/drafts/static-check-report.md`.
