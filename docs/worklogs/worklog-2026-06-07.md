# Worklog - 2026-06-07

## Context

The project scope was adjusted from a broad enterprise-grade Revit AI Agent toward a personal-use Revit assistant.

The user clarified:

- The tool is primarily for personal work.
- It may be shared with friends.
- It is open source and non-commercial.
- The project should not become unnecessarily large.
- Development should move faster.

## Decision

Use a smaller project direction:

- pyRevit remains the main implementation layer.
- C# DLL remains a lightweight official entry point.
- MCP and skills are deferred until the personal MVP is stable.
- Enterprise-grade workflow, permissions, release process, and broad platform design are not current priorities.

Safety rules remain:

- Do not directly modify production Revit models.
- Any model-changing feature requires preview/dry-run first.
- Apply tools require human confirmation, Revit Undo support, logs, and clear errors.

## Completed

- Replaced `docs/agent-development-roadmap.md` with a personal-agent roadmap.
- Reduced timeline expectations:
  - Personal MVP: 2-4 days.
  - Daily-use personal version: 1-2 weeks.
  - Small-share version: 2-4 weeks.
- Clarified that MCP, skills, Bridge, enterprise release, and complex permissions are optional later work.
- Added Hermes helper-agent task boundary in `docs/hermes-agent-brief.md`.
- Added `docs/drafts/README.md` for helper-agent draft documents.
- Added minimal error code documentation in `docs/error-codes.md`.
- Added filename validation and error codes to `ApplyMissingDoorWindowMarks`.
- Added explicit error-code prefixes to `ApplyMissingRoomNumbers`.
- Added `docs/hermes-deepseek-prompt.md` so the user can hand clear instructions to Hermes/DeepSeek.
- Reviewed Hermes branch output from `G:\Hermes Agent\YangAgent Revit\YangAgent Revit`.
- Imported Hermes draft docs into `docs/drafts/` and corrected wording around supported Revit versions, central models, process termination, and Undo validation.
- Reviewed Hermes button inventory audit. Result accepted: no functional inventory mismatch found; only minor documentation debt was reported.
- Reviewed Hermes troubleshooting audit. Result accepted: no unsafe advice found; two coverage gaps were identified in the summary draft.
- Imported Hermes troubleshooting summary patch and corrected process-closing wording to avoid unsafe task-kill guidance.
- Reviewed Hermes install-command audit. Result accepted: Hermes correctly identified two copy/paste command issues in quickstart.
- Added `docs/hermes-next-tasks.md` with bounded docs-only tasks for the next several days.
- Reviewed Hermes bulk Day1-Day5 output. Accepted as docs-only helper work after tightening Revit version, path, Undo, and error-code wording.
- Added `docs/hermes-capability-assessment.md` to define Hermes as a docs-only helper agent.

## Next Steps

1. Run existing pyRevit MVP through a sandbox model.
2. Fix the first blocking bug found in real use.
3. Keep C# DLL limited to launch/help/config/report folder entry points.
4. Add only immediately useful personal workflow features.
5. Keep daily worklogs short and practical.
6. Ask Hermes to branch from current baseline and work only inside allowed docs/drafts scope.
7. Review Hermes output before merging any branch.
8. If the drafts are useful, promote selected content into `README.md`, `docs/user-guide.md`, or `docs/troubleshooting.md`.
9. Assign Hermes a second bounded docs-only audit task if more capability testing is needed.
10. Ask Hermes to patch `hermes-troubleshooting-summary.md` with the two missing troubleshooting sections only.
11. Promote the corrected troubleshooting summary into the main troubleshooting/user-guide docs when ready.
12. Review Hermes future docs-only outputs against `docs/hermes-next-tasks.md`.
13. Continue mainline implementation separately; do not let Hermes edit core code.

## 2026-06-07 Later Mainline Batch

Completed:

- Added `docs/revit-version-support-plan.md` as the authoritative version boundary:
  - first phase targets Revit 2024-2027;
  - Revit 2011-2023 are deferred backlog targets;
  - current C# DLL skeleton remains Revit 2027 only.
- Kept Revit 2027 DLL small and stable, and replaced damaged Chinese placeholder strings in the DLL skeleton with ASCII English labels/messages.
- Added `YA-CS-STARTUP-001` for DLL startup failure messaging.
- Added explicit Undo / rollback notes to both low-risk apply logs:
  - `ApplyMissingDoorWindowMarks`;
  - `ApplyMissingRoomNumbers`.
- Added `tools/static_checks.py`, a read-only static check entry point that does not require Revit.
- Updated testing guidance and Hermes task guidance so Hermes can run read-only checks and produce review drafts.

Validation:

- Ran `python tools\static_checks.py --write-report`.
  - Result: 0 errors.
  - Remaining warnings are documentation command wording warnings for Hermes/Codex review.
- Ran `dotnet build src\YangAgent.Revit2027\YangAgent.Revit2027.csproj -o <temp build folder>`.
  - Result: build succeeded.
  - Known warnings: `MSB3277` Revit API / Microsoft.VisualBasic reference conflict warnings.
- Ran `git diff --check`.
  - Result: no whitespace errors; Git reported normal LF-to-CRLF warnings for edited C# files.
- Pending: manual Revit button and Undo validation in a sandbox model.

Notes:

- No `.rvt` files were added.
- No push was performed.
- Hermes remains blocked from core code, install scripts, addin templates, Revit execution, and model changes.

## 2026-06-07 Multi-Version Script Batch

Completed:

- Added generic DLL script entry points:
  - `scripts\build-revit-addin.ps1`
  - `scripts\install-revit-addin.ps1`
- Kept beginner-friendly Revit 2027 wrappers:
  - `scripts\build-revit2027-addin.ps1`
  - `scripts\install-revit2027-addin.ps1`
- Made Revit 2027 the only implemented C# DLL track.
- Added explicit planned-version failure for Revit 2024/2025/2026 with `YA-CS-VERSION-PLANNED`.
- Updated version support, DLL plan, error code, and Hermes task docs.

Validation:

- Ran PowerShell parser check for:
  - `scripts\build-revit-addin.ps1`
  - `scripts\install-revit-addin.ps1`
  - `scripts\build-revit2027-addin.ps1`
  - `scripts\install-revit2027-addin.ps1`
  - Result: all parsed successfully.
- Ran generic Revit 2027 temp build:
  - `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\build-revit-addin.ps1 -Version 2027 -OutputPath <temp>`
  - Result: build succeeded.
  - Known warnings: `MSB3277` Revit API / Microsoft.VisualBasic reference conflict warnings.
- Ran planned-version check:
  - `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\build-revit-addin.ps1 -Version 2024`
  - Result: stopped as expected with `YA-CS-VERSION-PLANNED`.
- Ran planned-version install check:
  - `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\install-revit-addin.ps1 -Version 2024`
  - Result: stopped as expected with `YA-CS-VERSION-PLANNED`.
- Reran `python tools\static_checks.py --write-report` after tightening the docs-command rule.
  - Result: 0 errors, 20 warnings.
  - Remaining warnings are docs/drafts cleanup work for Hermes/Codex review.

## 2026-06-07 Apply CSV Validation Batch

Completed:

- Rewrote `tools\static_checks.py` as ASCII-only Python to avoid future encoding drift.
- Added `tools\validate_apply_csv.py`, a read-only dry-run CSV validator for apply workflows.
- Added valid sample CSV fixtures:
  - `tests\fixtures\missing_room_numbers_valid.csv`
  - `tests\fixtures\missing_door_window_marks_valid.csv`
- Updated testing guidance, Hermes task guidance, and error code docs.

Validation:

- Ran `python -m py_compile tools\static_checks.py tools\validate_apply_csv.py`.
  - Result: passed.
- Ran `python tools\validate_apply_csv.py --kind room --csv tests\fixtures\missing_room_numbers_valid.csv`.
  - Result: passed, 2 applicable rows.
- Ran `python tools\validate_apply_csv.py --kind mark --csv tests\fixtures\missing_door_window_marks_valid.csv`.
  - Result: passed, 2 applicable rows.
- Ran `python tools\static_checks.py --write-report`.
  - Result: 0 errors, 17 warnings before handoff placeholder cleanup.

## 2026-06-07 Duplicate Apply CSV Guard Batch

Completed:

- Added duplicate `element_id` guard to both apply tools:
  - `ApplyMissingDoorWindowMarks` now stops before confirmation with `YA-APPLY-MARK-007`.
  - `ApplyMissingRoomNumbers` now stops before confirmation with `YA-APPLY-ROOM-007`.
- Upgraded offline duplicate CSV validation from warning to error:
  - `YA-APPLY-MARK-CSV-007`
  - `YA-APPLY-ROOM-CSV-007`
- Added duplicate CSV fixtures for negative tests.
- Updated error code, testing, and Hermes task docs.

Validation:

- Ran `python -m py_compile tools\static_checks.py tools\validate_apply_csv.py`.
  - Result: passed.
- Ran valid CSV fixtures:
  - `python tools\validate_apply_csv.py --kind room --csv tests\fixtures\missing_room_numbers_valid.csv`
  - `python tools\validate_apply_csv.py --kind mark --csv tests\fixtures\missing_door_window_marks_valid.csv`
  - Result: both passed.
- Ran duplicate CSV fixtures:
  - `python tools\validate_apply_csv.py --kind room --csv tests\fixtures\missing_room_numbers_duplicate.csv`
  - `python tools\validate_apply_csv.py --kind mark --csv tests\fixtures\missing_door_window_marks_duplicate.csv`
  - Result: both failed as expected with `YA-APPLY-*-CSV-007`.
- Ran `python tools\static_checks.py --write-report`.
  - Result: 0 errors, 15 warnings.
- Ran `git diff --check`.
  - Result: passed.

## 2026-06-07 Final Handoff Batch

Completed:

- Added `docs/handoff-new-chat-2026-06-07.md` as the current active handoff.
- Updated the older handoff to point new chats to the 2026-06-07 handoff first.
- Rewrote `docs/hermes-deepseek-prompt.md` for the current Hermes role:
  - read-only checks;
  - draft reports;
  - no core code;
  - no install/build/Revit execution;
  - no merge/push/pull.
- Updated `docs/hermes-agent-brief.md` and `docs/next-steps.md` with the current split.

Current status for next chat:

- Local `main` is ahead of `origin/main`.
- Do not push unless the user asks.
- Core next work: run/validate pyRevit MVP in a sandbox Revit model, then fix the first real blocker.
- Hermes next work: classify remaining static-check warnings and summarize apply CSV validation.

Validation:

- Ran `python tools\static_checks.py --write-report`.
  - Result: 0 errors, 15 warnings.
- Ran `python -m py_compile tools\static_checks.py tools\validate_apply_csv.py`.
  - Result: passed.
- Ran `git diff --check`.
  - Result: passed.

## 2026-06-07 pyRevit Preflight Batch

Completed:

- Added `tools/check_pyrevit_extension.py`, a read-only offline preflight for the pyRevit MVP bundle.
- The preflight checks:
  - required `.tab`, `.panel`, `.pulldown`, and `.pushbutton` metadata/assets;
  - ASCII folder naming for pyRevit cache stability;
  - UTF-8 readability for bundle metadata;
  - Python syntax compilation for all pyRevit `.py` files.
- Updated the active handoff so the next sandbox-model run starts with the new preflight command before opening Revit.

Validation:

- Ran `python -m py_compile tools\check_pyrevit_extension.py`.
  - Result: passed.
- Ran `python tools\check_pyrevit_extension.py`.
  - Result: 0 errors, 0 warnings.
- Rechecked pyRevit Python files with offline compile.
  - Result: all current pyRevit `.py` files compiled successfully.

Notes:

- This does not validate live Revit UI loading or Undo behavior.
- It is intended to fail early on bundle/layout mistakes before the next sandbox-model session.

## 2026-06-07 Document Authority Alignment Batch

Completed:

- Rewrote `README.md` to match the current personal-assistant scope.
- Marked `HANDOFF.md` as a legacy reference instead of a current authority source.
- Marked `docs/colleague-quickstart.md` as historical/optional shared-use reference.
- Marked `docs/revit-ai-agent-project-plan.md` as historical archive.
- Made the active document order explicit so future AI runs follow:
  - `docs/handoff-new-chat-2026-06-07.md`
  - `docs/next-steps.md`
  - latest relevant section in `docs/worklogs/worklog-2026-06-07.md`

Validation:

- Ran `python tools\static_checks.py --write-report`.
  - Result: 0 errors, 13 warnings.
  - Remaining warnings are older docs/drafts command wording items, not current mainline scope conflicts.
- Ran `git diff --check`.
  - Result: passed, with the existing CRLF warning on generated `docs/drafts/static-check-report.md`.

Notes:

- Current official positioning is personal-use Revit assistant, optionally shareable with friends.
- `Revit 2022` is no longer part of the current first-phase mainline.

## 2026-06-07 Hermes Review and Troubleshooting Command Batch

Completed:

- Reviewed the current Hermes draft audit direction against the active personal-MVP mainline.
- Confirmed the Hermes draft work remains useful as docs-only review material, not as an execution guide.
- Tightened `docs/troubleshooting.md` so the DLL-lock explanation now uses explicit `scripts\...` paths in prose.

Review verdict:

- Hermes work quality is acceptable as bounded draft/audit support.
- No evidence was found in the reviewed draft scope of core-code takeover, install/build execution, or model-operation expansion.
- Hermes command examples inside audit tables should be treated as quoted comparison evidence, not copy/paste execution guidance.

Validation:

- Reran `python tools\static_checks.py --write-report`.
  - Result: 0 errors, 12 warnings.
- Remaining warnings are older docs/drafts wording issues and not a blocker for current mainline work.

## 2026-06-07 Hermes Task Reset Batch

Completed:

- Replaced the older Hermes task list with a narrower current version in `docs/hermes-next-tasks.md`.
- Removed outdated task loops around already-resolved README/install cleanup.
- Recentered Hermes on:
  - static-check warning triage;
  - offline apply CSV review;
  - troubleshooting summary draft work;
  - legacy-doc indexing for Codex review.
- Updated the active handoff warning count from `15` to `11`.

Notes:

- Hermes remains a docs/read-only helper only.
- Core code, install/build flow, Revit execution, and model-changing work remain Codex-owned.

## 2026-06-07 Hermes Loop and Sandbox Runbook Batch

Completed:

- Added `docs/sandbox-pyrevit-mvp-runbook.md` as the standard live sandbox validation handoff.
- Updated `docs/next-steps.md` so live sandbox validation now points to the runbook instead of repeating loose steps only.
- Updated `docs/hermes-agent-brief.md` to document the Codex-controlled iteration loop:
  - task pack
  - Hermes report back
  - Codex review
  - next bounded task pack
- Updated the active handoff so future chats know to use the runbook and the Hermes task-pack control model.

Validation:

- The new runbook does not claim any live Revit step is already verified.
- The new Hermes loop keeps Hermes bounded to docs/read-only work and prevents scope drift.
