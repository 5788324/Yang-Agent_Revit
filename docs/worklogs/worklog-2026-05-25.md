# Worklog - 2026-05-25

## Context

Today continued the YangAgent Revit project with the goal of strengthening the foundation before adding more model-changing features.

Current project principles confirmed:

- Build the framework first; future model editing, plugins, MCP, Bridge, and skills should grow from it.
- Do not modify production Revit models directly.
- All model-changing features must follow dry-run -> human confirmation -> apply.
- Any apply feature must have undo, rollback, logs, and clear error feedback.
- The workflow must support Codex, Antigravity, Claude Code, human developers, and new AI chats after context compression.
- The user is treated as a code beginner and CAD/Revit beginner; AI should explain actions and risks clearly.
- Git pull/push should not be frequent; the user will tell AI when to upload or download from Git.

## Completed

- Confirmed the repository was up to date earlier in the session.
- Preserved the user's update to `docs/handoff-new-chat-2026-05-23.md`.
- Added DLL lock detection to:
  - `scripts/build-revit2027-addin.ps1`
  - `scripts/install-revit2027-addin.ps1`
- Verified both scripts now fail early with a clear message when Revit locks `YangAgent.Revit2027.dll`.
- Verified C# code still builds successfully to a temporary output directory while Revit is running.
- Added DLL lock troubleshooting notes to `docs/troubleshooting.md`.
- Added `docs/agent-development-roadmap.md` to capture:
  - timeline estimate,
  - next few days of work,
  - multi-AI collaboration rules,
  - rollback and error feedback requirements,
  - code/CAD beginner communication rules,
  - skills and MCP server rules,
  - missing documentation list.

## Validation

- `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build-revit2027-addin.ps1`
  - Expected failure while Revit is open.
  - Reported locked DLL and Revit process id.
- `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\install-revit2027-addin.ps1`
  - Expected failure while Revit is open.
  - Reported locked DLL and Revit process id.
- `dotnet build .\src\YangAgent.Revit2027\YangAgent.Revit2027.csproj -c Debug -o C:\tmp\YangAgent_Revit2027_build_check`
  - Build succeeded.
  - Existing `MSB3277` Revit API reference warnings remain.

## Known Issues

- Revit process was running and locking the default DLL output.
- `git status` prints a local warning about `C:\Users\YANG/.config/git/ignore` permission, but repository status is still readable.
- Current Revit in-app DLL button validation still requires manual testing after Revit restart.

## Next Steps

1. Close Revit 2027, then rerun DLL build and install scripts.
2. Restart Revit 2027 and manually verify DLL buttons.
3. Run pyRevit regression checklist in a sandbox model.
4. Create `docs/error-codes.md`.
5. Create `docs/rollback-and-recovery.md`.
6. Create `docs/test-models.md`.
7. Continue keeping daily worklog and handoff notes updated.
