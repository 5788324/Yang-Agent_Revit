# Worklog - 2026-06-08

## Context

The user approved the next-stage Hermes upgrade plan:

- Hermes may move from docs-only support to bounded read-only code review.
- Codex keeps control of implementation, quality gates, and final decisions.
- Mainline priority remains the first real pyRevit MVP sandbox run and the first live blocker after that run.

## Completed

- Upgraded `docs/hermes-agent-brief.md` to explicitly allow bounded read-only code review of:
  - `pyrevit/**/script.py`
  - `src/**`
  - `tools/**`
  - `tests/**`
- Kept Hermes blocked from implementation, Revit execution, and install/build flow.
- Rewrote `docs/hermes-next-tasks.md` around a code-review task-pack model.
- Assigned Round 1 Hermes review tasks:
  - apply tools code review;
  - sandbox runbook feature review.
- Added `docs/sandbox-pyrevit-mvp-feedback-template.md` for first live-run blocker capture.
- Updated `docs/sandbox-pyrevit-mvp-runbook.md` to point failed live runs to the feedback template.
- Added `tools/run_sandbox_preflight.py` so the offline sandbox gate can run as one repeatable command instead of a manual multi-command sequence.
- Added shared apply helper module `pyrevit/YangAgent.extension/lib/yang_agent_apply.py` so duplicated CSV/apply utility logic no longer needs to be fixed in two button scripts.
- Updated `docs/next-steps.md` and `docs/handoff-new-chat-2026-06-07.md` so future runs use:
  - the sandbox runbook;
  - the sandbox feedback template;
  - the Hermes task-pack control model.
- Marked Hermes Round 1 as assigned in `docs/hermes-next-tasks.md`.
- Tightened lingering wording so Hermes is described consistently as read-only checks + draft docs + bounded read-only code review, never implementation.
- Updated `CHANGELOG.md` for the new Hermes review mode and feedback template.

## Validation

- Ran `python tools\check_pyrevit_extension.py`.
  - Result: 0 errors, 0 warnings after changing syntax compilation to a pure read-only `compile(...)` path instead of writing `__pycache__`.
- Ran `python tools\run_sandbox_preflight.py --write-report`.
  - Result: all preflight steps passed.
  - Report: `docs/drafts/sandbox-preflight-report.md`.
- Ran `python tools\static_checks.py --write-report`.
  - Result: 0 errors, 11 warnings.
  - Remaining warnings are older docs/drafts items, not a blocker for the current mainline.
- Ran `git diff --check`.
  - Result: passed, with the existing CRLF warning on generated `docs/drafts/static-check-report.md`.

## Quality Gate

- Hermes is now allowed to read code for review, but not to change implementation files.
- Hermes findings remain advisory only; Codex retains final judgment.
- No live Revit step is marked verified in these changes.

## Next Steps

1. Wait for the first Round 1 Hermes code-review reports.
2. Review Hermes output and accept or reject findings explicitly.
3. Continue preparing the first real sandbox-model run.
4. Use `docs/sandbox-pyrevit-mvp-feedback-template.md` to capture the first live blocker.
5. Fix only that first blocker before expanding scope.
