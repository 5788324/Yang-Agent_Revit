# pyRevit 工具生成模板

请根据以下需求生成 pyRevit 工具。

要求：

1. 先说明是否会修改模型。
2. 如果修改模型，必须先提供 dry-run。
3. 所有修改必须放在 Transaction 中。
4. 批量修改前必须输出影响元素数量。
5. 代码兼容 IronPython 2.7 风格。
6. 不使用 f-string、类型注解、walrus 运算符。
7. 输出完整 pyRevit bundle 目录结构。
8. 输出使用说明和测试步骤。

需求：

```text
{用户需求}
```
